-- █ ARAKS HUB AIMBOT + COMBAT CONTROLLER INTEGRATION (VIRTUAL CLICK FIX)
-- ✅ تحويل طريقة النقر البرمجي الفعلي لمحاكاة الضغط فوق الخصم مباشرة
-- ✅ تكبير حجم الـ Hitbox السري والشفاف إلى 95 لجلد عنيف
-- ✅ تتبع موقع الخصم على الشاشة (WorldToViewportPoint) وضغطه برمجياً
-- ✅ أداء سلس وبدون تجميد للأزرار أو لاق عند الاقتراب

local AimbotModule = {}

do
    local Players = game:GetService("Players")
    local player = Players.LocalPlayer
    local RunService = game:GetService("RunService")
    local Camera = workspace.CurrentCamera
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local UserInputService = game:GetService("UserInputService")
    local VirtualUser = game:GetService("VirtualUser") -- الموديول المسؤول عن محاكاة النقر الفعلي
    
    -- استدعاء موديول اللعبة المسؤول عن كشف الضربات والضرر الفعلي
    local CombatController = nil
    pcall(function()
        CombatController = require(ReplicatedStorage:WaitForChild("Controllers"):WaitForChild("CombatController"))
    end)

    getgenv().AimbotActive = true -- تفعيل السكربت تلقائياً

    local AimbotConfig = {
        TargetPart = "HumanoidRootPart",
        MaxDistance = 1000,  -- مدى 1000 متر
        HitboxSize = 95,     -- الحجم المكبّر
        Prediction = 0.07,   -- تنبؤ متوازن وسريع للمهارات
    }

    -- ==================================================
    -- نظام التحقق من العدو (قراصنة ضد الكل / بحرية ضد قراصنة)
    -- ==================================================
    local function IsValidEnemy(plr)
        if not plr or plr == player then return false end
        if not plr:IsA("Player") then return false end
        
        local char = plr.Character
        if not char then return false end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hum or hum.Health <= 0 then return false end
        
        local userId = plr.UserId
        if userId <= 0 or userId == nil then return false end
        
        local myTeam = player.Team
        local targetTeam = plr.Team
        
        if myTeam and myTeam.Name == "Pirates" then return true end
        if myTeam and myTeam.Name == "Marines" then
            if targetTeam and targetTeam.Name == "Pirates" then return true else return false end
        end
        return true
    end
    
    local CurrentTarget = nil
    local CurrentTargetPos = nil
    local lastTarget = nil
    
    -- مظهر نيون بنفسجي خارق يظهر الخصم من خلف الجدران
    local PurpleChams = Instance.new("BoxHandleAdornment")
    PurpleChams.Size = Vector3.new(4, 6, 4)
    PurpleChams.Color3 = Color3.fromRGB(165, 0, 255)
    PurpleChams.AlwaysOnTop = true
    PurpleChams.ZIndex = 10
    PurpleChams.Transparency = 0.4
    PurpleChams.Visible = false
    PurpleChams.Parent = game:GetService("CoreGui")
    
    -- الحصول على أقرب لاعب
    local function GetNearestEnemy()
        local char = player.Character
        if not char then return nil end
        
        local myHrp = char:FindFirstChild("HumanoidRootPart")
        if not myHrp then return nil end
        
        local nearest = nil
        local nearestDist = AimbotConfig.MaxDistance
        local myPos = myHrp.Position
        
        for _, pl in ipairs(Players:GetPlayers()) do
            if IsValidEnemy(pl) then
                local hrp = pl.Character and pl.Character:FindFirstChild("HumanoidRootPart")
                local hum = pl.Character and pl.Character:FindFirstChildOfClass("Humanoid")
                
                if hrp and hum and hum.Health > 0 then
                    local dist = (hrp.Position - myPos).Magnitude
                    if dist < nearestDist then
                        nearestDist = dist
                        nearest = pl
                    end
                end
            end
        end
        return nearest
    end
    
    -- تطبيق الـ Hitbox المطور والشفاف
    local function ApplyHitboxSystem(plr)
        if not plr or not plr.Character then return end
        local rootPart = plr.Character:FindFirstChild("HumanoidRootPart")
        local humanoid = plr.Character:FindFirstChildOfClass("Humanoid")
        
        if rootPart and humanoid and humanoid.Health > 0 then
            pcall(function()
                rootPart.Size = Vector3.new(AimbotConfig.HitboxSize, AimbotConfig.HitboxSize, AimbotConfig.HitboxSize)
                rootPart.CanCollide = false
                rootPart.Transparency = 1.0
                rootPart.Material = Enum.Material.ForceField
                
                PurpleChams.Adornee = rootPart
                PurpleChams.Visible = true
                
                if CombatController and type(CombatController.runHitDetection) == "function" then
                    task.spawn(function()
                        pcall(function() CombatController.runHitDetection(plr.Character) end)
                    end)
                end
            end)
        end
    end
    
    -- إعادة ضبط الـ Hitbox عند إلغاء الهدف
    local function ResetHitbox(plr)
        if not plr or not plr.Character then return end
        local rootPart = plr.Character:FindFirstChild("HumanoidRootPart")
        if rootPart then
            pcall(function()
                rootPart.Size = Vector3.new(2, 2, 2)
                rootPart.CanCollide = true
                rootPart.Transparency = 0
            end)
        end
        if rootPart and PurpleChams.Adornee == rootPart then
            PurpleChams.Visible = false
            PurpleChams.Adornee = nil
        end
    end
    
    -- ==================================================
    -- AIMBOT UPDATE LOOP + نظام النقرات الافتراضية الموجهة
    -- ==================================================
    RunService.RenderStepped:Connect(function()
        if not getgenv().AimbotActive then
            if lastTarget then ResetHitbox(lastTarget); lastTarget = nil end
            CurrentTarget = nil; CurrentTargetPos = nil; PurpleChams.Visible = false
            return
        end
        
        local target = GetNearestEnemy()
        
        if target and target.Character then
            local hrp = target.Character:FindFirstChild(AimbotConfig.TargetPart)
            
            if hrp then
                if target ~= lastTarget then
                    if lastTarget then ResetHitbox(lastTarget) end
                    lastTarget = target
                    ApplyHitboxSystem(target)
                end
                
                CurrentTarget = target
                local velocity = hrp.AssemblyLinearVelocity or Vector3.new(0,0,0)
                CurrentTargetPos = hrp.Position + (velocity * AimbotConfig.Prediction)
                
                -- طريقتك المطلوبة: تتبع موقع الخصم ومحاكاة النقر فوقه مباشرة عند تفعيل الضرب
                local myChar = player.Character
                local tool = myChar and myChar:FindFirstChildOfClass("Tool")
                local isClicking = UserInputService:IsMouseButtonPressed(Enum.MouseButton1) or #UserInputService:GetMouseLocation() > 0
                
                if tool and isClicking and not UserInputService:GetFocusedTextBox() then
                    -- تحويل إحداثيات الخصم ثلاثية الأبعاد (X,Y,Z) إلى نقطة ثنائية الأبعاد على الشاشة (X,Y)
                    local screenPos, onScreen = Camera:WorldToViewportPoint(CurrentTargetPos)
                    
                    if onScreen then
                        -- محاكاة نقرة حقيقية بواسطة محرك اللعبة فوق الخصم مباشرة في نفس الفريم
                        pcall(function()
                            VirtualUser:Button1Down(Vector2.new(screenPos.X, screenPos.Y), Camera.CFrame)
                        end)
                    end
                end
            else
                CurrentTarget = nil; CurrentTargetPos = nil; PurpleChams.Visible = false
            end
        else
            if lastTarget then ResetHitbox(lastTarget); lastTarget = nil end
            CurrentTarget = nil; CurrentTargetPos = nil; PurpleChams.Visible = false
        end
    end)
    
    -- تثبيت دوري للهيت بوكس المطور
    task.spawn(function()
        while task.wait(0.15) do
            if getgenv().AimbotActive and lastTarget then
                if lastTarget:IsA("Player") then ApplyHitboxSystem(lastTarget) end
            end
        end
    end)
    
    -- الـ Silent Aim الأصلي كخط دفاع احتياطي متناسق
    local oldIndex
    oldIndex = hookmetamethod(game, "__index", function(self, index)
        if index == "Hit" and getgenv().AimbotActive and not checkcaller() then
            if CurrentTarget and CurrentTarget.Character then
                local targetPart = CurrentTarget.Character:FindFirstChild(AimbotConfig.TargetPart)
                if targetPart then return targetPart.CFrame end
            end
        end
        return oldIndex(self, index)
    end)
    
    Players.PlayerRemoving:Connect(function(plr)
        if lastTarget == plr then
            ResetHitbox(plr)
            lastTarget = nil; CurrentTarget = nil; PurpleChams.Visible = false
        end
    end)
    
    function AimbotModule:SetState(state)
        getgenv().AimbotActive = state
        if not state and lastTarget then
            ResetHitbox(lastTarget)
            lastTarget = nil; CurrentTarget = nil; PurpleChams.Visible = false
        end
    end
    
    print("[ARAKS HUB] AIMBOT REBUILT VIA VIRTUAL SCREEN CLICKING - READY!")
end

return AimbotModule
