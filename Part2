-- GUI UPDATE PANEL FULL
local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local Button = Instance.new("TextButton")
local CopyMsg = Instance.new("TextLabel")
local UICorner = Instance.new("UICorner")
local UIStroke = Instance.new("UIStroke")
local Background = Instance.new("ImageLabel")

ScreenGui.Parent = game.CoreGui

-- الإطار
Frame.Parent = ScreenGui
Frame.Size = UDim2.new(0, 300, 0, 300)
Frame.Position = UDim2.new(0.5, -150, 0.5, -150)
Frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
UICorner.Parent = Frame

-- RGB Border
UIStroke.Parent = Frame
UIStroke.Thickness = 3
UIStroke.Color = Color3.fromRGB(180, 0, 255)

-- صورة الخلفية
Background.Parent = Frame
Background.Size = UDim2.new(1,0,1,0)
Background.Image = "rbxassetid://10957088649"
Background.BackgroundTransparency = 0.3
Background.ZIndex = 0

-- العنوان
Title.Parent = Frame
Title.Size = UDim2.new(1,0,0,80)
Title.Text = "تم تنزيل إصدار جديد\nNew Update Released"
Title.TextColor3 = Color3.fromRGB(255,255,255)
Title.BackgroundTransparency = 1
Title.TextScaled = true
Title.ZIndex = 2

-- الزر
Button.Parent = Frame
Button.Size = UDim2.new(0.8,0,0,50)
Button.Position = UDim2.new(0.1,0,0.5,0)
Button.Text = "حمل من هنا / Download"
Button.BackgroundColor3 = Color3.fromRGB(40,40,40)
Button.TextColor3 = Color3.fromRGB(255,255,255)
Button.TextScaled = true
Button.ZIndex = 2

-- رسالة النسخ
CopyMsg.Parent = Frame
CopyMsg.Size = UDim2.new(1,0,0,40)
CopyMsg.Position = UDim2.new(0,0,0.8,0)
CopyMsg.Text = ""
CopyMsg.TextColor3 = Color3.fromRGB(0,255,0)
CopyMsg.BackgroundTransparency = 1
CopyMsg.TextScaled = true
CopyMsg.ZIndex = 2

-- الرابط الجديد
local link = "https://sub2unlock.io/jM8ht"

-- زر النسخ
Button.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(link)
        CopyMsg.Text = "✔ تم النسخ | Copied!"
    else
        CopyMsg.Text = "❌ Clipboard غير مدعوم"
    end
end)

print("GUI Loaded")
